

# Notas:

```
Options:
      --help     Show help                                          [boolean]
      --version  Show version number                                [boolean]
  -b, --base     Base de la tabla de multiplicar          [number] [required]
  -l, --listar   Muestra la lista en consola       [boolean] [default: false]
  -h, --hasta    Número máximo en tabla de multiplicar [number] [default: 10]

```